package com.devtorres.core_utils

object RegexPatterns {

    /**
     * Expresión regular para validar que un valor sea un número
     */
    const val IS_NUMERIC = "^[0-9]*\\.?[0-9]*$"

    const val IS_DIGIT = "^[0-9]*$"
}