package com.devtorres.fit_app.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales extraídos de la aplicación React
val Primary = Color(0xFFFCEC52) // Amarillo
val OnPrimary = Color(0xFF000000) // Negro
val Secondary = Color(0xFF4A90E2) // Azul
val OnSecondary = Color(0xFFFFFFFF) // Blanco
val Tertiary = Color(0xFFFF6F61) // Coral/Rojo
val OnTertiary = Color(0xFFFFFFFF) // Blanco

// Colores de fondo y superficie para tema oscuro
val DarkBackground = Color(0xFF444545) // Gris oscuro
val DarkSurface = Color(0xFF4D4E4E) // Gris oscuro (ligeramente más claro)
val OnDarkBackground = Color(0xFFFAFAFA) // Casi blanco
val OnDarkSurface = Color(0xFFFAFAFA) // Casi blanco

// Colores adicionales para el tema oscuro
val DarkSurfaceVariant = Color(0xFF333434) // Más oscuro que la superficie
val OnDarkSurfaceVariant = Color(0xFFE0E0E0) // Gris claro
val DarkOutline = Color(0xFF595959) // Gris medio para bordes
val DarkMuted = Color(0xFF333333) // Gris oscuro para elementos atenuados
val OnDarkMuted = Color(0xFFA3A3A3) // Gris claro para texto atenuado

// Contenedores para colores primarios
val PrimaryContainer = Color(0xFF635E21) // Versión más oscura del amarillo primario
val OnPrimaryContainer = Color(0xFFFCEC52) // Amarillo primario
val SecondaryContainer = Color(0xFF1E3A5A) // Versión más oscura del azul secundario
val OnSecondaryContainer = Color(0xFF4A90E2) // Azul secundario
val TertiaryContainer = Color(0xFF662C27) // Versión más oscura del coral terciario
val OnTertiaryContainer = Color(0xFFFF6F61) // Coral terciario

// Colores de error
val Error = Color(0xFFBA1F33) // Rojo error actualizado
val OnError = Color(0xFFFFFFFF) // Blanco sobre error
val ErrorContainer = Color(0xFF5D0F19) // Contenedor de error (versión más oscura del nuevo rojo)
val OnErrorContainer = Color(0xFFFFB3BD) // Texto sobre contenedor de error (versión más clara del nuevo rojo)

// Colores inversos
val InverseSurface = Color(0xFFE0E0E0) // Superficie inversa (gris claro)
val InverseOnSurface = Color(0xFF333333) // Texto sobre superficie inversa (gris oscuro)
val InversePrimary = Color(0xFF635E21) // Primario inverso (amarillo oscuro)

// Colores adicionales requeridos por Material 3
val Scrim = Color(0x52000000) // Color semitransparente para capas de oscurecimiento
val SurfaceTint = DarkSurface // Tinte de superficie
val SurfaceBright = Color(0xFF5A5B5B) // Superficie brillante
val SurfaceContainer = Color(0xFF464747) // Contenedor de superficie
val SurfaceContainerHigh = Color(0xFF525353) // Contenedor de superficie alto
val SurfaceContainerHighest = Color(0xFF5F6060) // Contenedor de superficie más alto
val SurfaceContainerLow = Color(0xFF3D3E3E) // Contenedor de superficie bajo
val SurfaceContainerLowest = Color(0xFF333434) // Contenedor de superficie más bajo
val SurfaceDim = Color(0xFF3A3B3B) // Superficie tenue

// Colores extendidos Rosa
val Pink = Color(0xFFE91E63) // Rosa base (equivalente a Pink 500 en Material)
val OnPink = Color(0xFFFFFFFF) // Texto sobre rosa
val PinkContainer = Color(0xFF741042) // Contenedor rosa (versión más oscura)
val OnPinkContainer = Color(0xFFFFB1CF) // Texto sobre contenedor rosa (versión más clara)

// Colores extendidos verde
val Green = Color(0xFF03CEA4) // Rosa base (equivalente a Pink 500 en Material)
val OnGreen = Color(0xFF000000) // Texto sobre rosa
val GreenContainer = Color(0xFF0A8C71) // Contenedor rosa (versión más oscura)
val OnGreenContainer = Color(0xFF63EAD3) // Texto sobre contenedor rosa (versión más clara)